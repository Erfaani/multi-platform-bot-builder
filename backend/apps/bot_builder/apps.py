from django.apps import AppConfig


class BotBuilderConfig(AppConfig):
    name = "apps.bot_builder"
    label = "bot_builder"
    verbose_name = "Chat-native bot ordering (the platform's own builder bot)"
